# Ragent 文档入库与业务管理流程树与实现指南

> 本文档作为 RAG 优化流程树与实现指南的配套篇，覆盖文档入库、知识库管理、意图树管理、文档定时刷新和分块管理五大业务模块，帮助开发者理解"数据如何进入 RAG 系统"的完整链路。

---

## 一、整体业务流程树

**主线一：文档入库（用户主动上传）**

```
用户上传文件
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. 文件上传与元数据存储 (KnowledgeDocumentServiceImpl.upload)               │
│    ├── 文件存储到 OSS/NFS                                                    │
│    ├── 写入文档元数据记录 (状态=PENDING)                                     │
│    └── 发送 MQ 消息触发分块任务                                              │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. 摄入引擎执行 (IngestionEngine.execute)                                   │
│    ├── FetcherNode  - 获取文件内容                                           │
│    ├── ParserNode   - 解析文档为结构化文本                                   │
│    ├── ChunkerNode  - 文本分块                                               │
│    ├── EnricherNode - LLM 增强（可选节点）                                   │
│    └── IndexerNode  - 向量化 + 存储到 Milvus                                │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. 落库与状态更新 (KnowledgeDocumentServiceImpl.chunkDocument)              │
│    ├── 批量写入分块数据 (t_knowledge_chunk)                                  │
│    ├── 记录分块日志   (t_knowledge_document_chunk_log)                       │
│    └── 更新文档状态   (RUNNING → SUCCESS / FAILED)                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

**主线二：文档定时刷新（系统自动拉取）**

```
定时扫描任务（每10秒）
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ KnowledgeDocumentScheduleJob.scan()                                         │
│    ├── 查询 enabled=1 且 nextRunTime <= now 的调度记录                       │
│    ├── 数据库乐观锁抢占（多实例部署防重复执行）                              │
│    └── 提交 executeSchedule() 到异步线程池                                   │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ KnowledgeDocumentScheduleJob.executeSchedule()                              │
│    ├── 拉取远程文件（HTTP / 飞书 / S3）                                      │
│    ├── 内容未变化（ETag/Hash 对比）→ SKIPPED                                 │
│    ├── 上传新文件到 OSS                                                      │
│    ├── 调用 chunkDocument() 重新入库                                         │
│    └── 清理旧文件，更新执行记录                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、文档入库流程 (IngestionEngine)

**核心类：**

| 类名 | 职责 |
|------|------|
| `KnowledgeDocumentServiceImpl` | 上传入口，负责文件存储、元数据写入、触发分块 |
| `IngestionEngine` | 流水线执行引擎，按节点链式执行 |
| `FetcherNode` | 从 OSS/本地/HTTP 获取文件内容 |
| `ParserNode` / `TikaDocumentParser` | 解析 PDF/Word/Excel/Markdown 为纯文本 |
| `ChunkerNode` | 文本分块，支持固定大小和结构感知两种策略 |
| `EnricherNode` | 调用 LLM 对分块进行语义增强（可选节点） |
| `IndexerNode` | 向量化（调用 Embedding 模型）并写入 Milvus |
| `KnowledgeChunkService` | 分块数据落库（MySQL）及向量索引管理 |

**优化点：**

| 优化项 | 说明 | 核心代码位置 |
|--------|------|--------------|
| MQ 解耦 | 上传返回后异步执行分块，不阻塞用户 | `KnowledgeDocumentChunkConsumer` |
| 流水线引擎 | 节点链式可配置，新增节点无需改主流程 | `IngestionEngine.executeChain()` |
| 节点条件跳过 | 每个节点支持条件表达式，满足条件才执行 | `ConditionEvaluator.evaluate()` |
| 分块策略可选 | FixedSize（按字数）/ StructureAware（按标题/段落） | `ChunkingStrategy` 实现类 |
| 内容哈希去重 | 分块写入前比对 contentHash，相同内容不重复入库 | `KnowledgeChunkDO.contentHash` |
| 崩溃恢复 | 每分钟检查卡在 RUNNING 超时的文档，重置为 FAILED | `recoverStuckRunningDocuments()` |

**代码示例：**

> **业务例子（伪代码）**
>
> ```
> 输入：
>   kbId = "kb_001"
>   文件：《员工手册2024版.pdf》，大小 2MB
>
> 处理过程：
>
>   第1步：文件上传（KnowledgeDocumentServiceImpl.upload）
>   - 文件存到 OSS：/kb_001/docs/员工手册2024版.pdf
>   - 写入 t_knowledge_document：
>     id="doc_001", kbId="kb_001", status=PENDING, fileUrl="oss://..."
>   - 发送 MQ 消息：KnowledgeDocumentChunkEvent{docId="doc_001"}
>
>   第2步：Consumer 收到消息，开始分块（IngestionEngine）
>   流水线节点链：FetcherNode → ParserNode → ChunkerNode → IndexerNode
>
>   FetcherNode：
>   - 从 OSS 下载 PDF 文件 → InputStream
>
>   ParserNode：
>   - 检测 MIME 类型：application/pdf
>   - 选择解析器：TikaDocumentParser
>   - 解析结果：纯文本 12000 字
>
>   ChunkerNode：
>   - 分块策略：FixedSize，chunkSize=500字，overlap=50字
>   - 分块结果：25个 VectorChunk
>
>   IndexerNode：
>   - 调用 Embedding 模型，批量生成 25个向量（1536维）
>   - 写入 Milvus Collection："kb_001"
>
>   第3步：落库
>   - 批量写入 t_knowledge_chunk：25条记录
>   - 更新 t_knowledge_document：status=SUCCESS, chunkCount=25
> ```
>
> **这段代码做什么？**
>
> 这是"流水线执行引擎"的核心逻辑，将文档处理抽象为可配置的节点链：
> 1. **节点发现**：启动时扫描所有 `IngestionNode` Bean，按 nodeType 注册到 nodeMap
> 2. **起始节点定位**：找出没有被其他节点 `nextNodeId` 引用的节点（即链头）
> 3. **链式执行**：从起始节点出发，执行完后按 `nextNodeId` 移动到下一节点
> 4. **条件判断**：每个节点可配置条件表达式，条件不满足则跳过该节点继续往后走
> 5. **失败中断**：任意节点失败时，标记 `context.status = FAILED` 并停止链式执行

```java
// IngestionEngine.java:60-87 - 流水线执行核心
public IngestionContext execute(PipelineDefinition pipeline, IngestionContext context) {
    context.setStatus(IngestionStatus.RUNNING);

    // 构建节点配置映射：nodeId → NodeConfig
    Map<String, NodeConfig> nodeConfigMap = buildNodeConfigMap(pipeline.getNodes());

    // 验证流水线（检测环路、检测悬空引用）
    validatePipeline(nodeConfigMap);

    // 找到起始节点（没有被任何 nextNodeId 引用的节点）
    String startNodeId = findStartNode(nodeConfigMap);

    // 从起始节点开始链式执行
    executeChain(startNodeId, nodeConfigMap, context);

    if (context.getStatus() == IngestionStatus.RUNNING) {
        context.setStatus(IngestionStatus.COMPLETED);
    }
    return context;
}

// IngestionEngine.java:158-200 - 链式执行
private void executeChain(String nodeId, Map<String, NodeConfig> nodeConfigMap, IngestionContext context) {
    String currentNodeId = nodeId;
    while (currentNodeId != null) {
        NodeConfig config = nodeConfigMap.get(currentNodeId);

        // 执行当前节点（含条件判断）
        NodeResult result = executeNode(context, config);

        if (!result.isSuccess()) {
            context.setStatus(IngestionStatus.FAILED);
            context.setError(result.getError());
            break;  // 节点失败，终止链式执行
        }

        if (!result.isShouldContinue()) {
            break;  // 节点主动要求停止（如内容为空时 ParserNode 返回 shouldContinue=false）
        }

        // 移动到下一个节点
        currentNodeId = config.getNextNodeId();
    }
}

// IngestionEngine.java:205-261 - 执行单个节点（含条件判断 + 日志）
private NodeResult executeNode(IngestionContext context, NodeConfig nodeConfig) {
    String nodeType = nodeConfig.getNodeType();
    IngestionNode node = nodeMap.get(nodeType);

    // 条件判断：满足条件才执行，否则 skip（跳过但不中断链）
    if (nodeConfig.getCondition() != null && !nodeConfig.getCondition().isNull()) {
        if (!conditionEvaluator.evaluate(context, nodeConfig.getCondition())) {
            return NodeResult.skip("条件未满足");
        }
    }

    long start = System.currentTimeMillis();
    try {
        NodeResult result = node.execute(context, nodeConfig);
        // 记录节点日志（耗时、状态、输出摘要）
        context.getLogs().add(NodeLog.builder()
                .nodeId(nodeConfig.getNodeId())
                .nodeType(nodeType)
                .durationMs(System.currentTimeMillis() - start)
                .success(result.isSuccess())
                .build());
        return result;
    } catch (Exception e) {
        return NodeResult.fail(e);  // 异常 = 失败，不向上抛出，由链式执行处理
    }
}
```

**使用场景：** 将 PDF、Word、Markdown 等格式文件解析分块后存入 Milvus，供 RAG 检索使用。

**优化优点：**
- 流水线设计让每个节点职责单一，新增文档格式只需新增 Parser Bean，无需改主流程
- 条件跳过让同一流水线支持多种处理模式（有/无 LLM 增强）
- 节点日志链路清晰，每个节点的耗时和输出摘要都可追溯

---

## 三、知识库管理 (KnowledgeBaseService)

知识库是 RAG 系统的顶层容器，每个知识库对应 Milvus 中的一个 Collection。创建时需指定嵌入模型，后续所有入库文档都用该模型生成向量。

**API 接口：**

| 操作 | 方法 | 说明 |
|------|------|------|
| 创建 | `create(KnowledgeBaseCreateRequest)` | 创建知识库，同步创建 Milvus Collection |
| 更新 | `update(KnowledgeBaseUpdateRequest)` | 更新名称、描述等元数据 |
| 删除 | `delete(kbId)` | 删除知识库及其所有文档、分块、向量 |
| 详情 | `queryById(kbId)` | 查询知识库详情 |
| 分页 | `pageQuery(KnowledgeBasePageRequest)` | 分页列表查询 |

**数据模型：**

```java
// KnowledgeBaseDO - 知识库主表 (t_knowledge_base)
private String id;              // 知识库ID
private String name;            // 名称，如"企业内部规范"
private String embeddingModel;  // 嵌入模型ID，决定向量维度
private String collectionName;  // Milvus Collection 名，全局唯一
private String createdBy;
private Date createTime;
```

> **注意：** `embeddingModel` 一旦设定不可更改，因为 Milvus Collection 的向量维度在创建时固定。若需更换模型，必须重建知识库并重新入库所有文档。

---

## 四、意图树管理 (IntentTreeService)

意图树定义了"用户问题 → 对应知识库"的映射关系。树形结构分三级：领域(DOMAIN) → 类目(CATEGORY) → 话题(TOPIC)，叶子节点关联具体知识库或 MCP 工具。

**CRUD 接口：**

| 操作 | 方法 | 说明 |
|------|------|------|
| 创建节点 | `createNode(IntentNodeCreateRequest)` | 新增意图节点（含层级关系） |
| 更新节点 | `updateNode(id, request)` | 修改名称、描述、示例等 |
| 删除节点 | `deleteNode(id)` | 逻辑删除，子节点自动失效 |
| 查询全树 | `getFullTree()` | 返回完整树形结构（含所有子节点） |
| 批量操作 | `batchEnableNodes()` / `batchDisableNodes()` | 批量启用/禁用节点 |
| 初始化 | `initFromFactory()` | 从 IntentTreeFactory 加载默认树结构到数据库 |

**意图树 Redis 缓存刷新策略：**

| 优化项 | 说明 | 核心代码位置 |
|--------|------|--------------|
| Redis 缓存 | 意图树加载到 Redis，避免每次请求查 MySQL | `DefaultIntentClassifier.loadIntentTreeData()` |
| 懒加载 | 缓存不存在时自动从数据库加载并写入缓存 | `loadIntentTreeFromDB()` |
| 主动失效 | 修改节点后主动删除 Redis 缓存，下次请求重建 | `IntentTreeServiceImpl.evictCache()` |
| TTL 保底 | 缓存有 TTL 自动过期，防止长时间不变导致缓存陈旧 | 配置项 `rag.intent.cache-ttl-minutes` |

**代码示例：**

> **业务例子（伪代码）**
>
> ```
> 场景：运维在意图树中添加了"IT支持 > 设备 > 打印机"新节点
>
> 第1步：调用 createNode()
> - 写入 t_intent_node 记录（parentId=设备节点，kind=KB，kbId="it-kb"）
> - 调用 evictCache()：删除 Redis Key "rag:intent:tree"
>
> 第2步：下一次意图分类请求到来（用户问"打印机坏了怎么处理"）
> - DefaultIntentClassifier.loadIntentTreeData()
> - 查 Redis → 缓存已失效，Cache Miss
> - 查 MySQL → 加载全量意图树（含新的"打印机"节点）
> - 写入 Redis 缓存，TTL=30分钟
>
> 第3步：LLM 分类
> - 叶子节点列表中包含"打印机"节点
> - LLM 对"打印机坏了怎么处理"打分：打印机节点 score=0.9
> - 命中 IT知识库，定向检索打印机相关文档
>
> 输出：新节点"打印机"已生效，下次分类时 LLM 能识别到
> ```
>
> **这段代码做什么？**
>
> 意图树缓存采用"写时失效 + 读时重建"策略：
> 1. **写时失效**：每次增删改意图节点，主动清除 Redis 中的意图树缓存
> 2. **读时重建**：意图分类器检测到缓存 Miss 时，从 MySQL 重新加载完整意图树并缓存到 Redis
> 3. **TTL 保底**：即使主动失效遗漏（如直连数据库修改），TTL 到期后也会自动更新，不会永久使用旧数据

```java
// DefaultIntentClassifier.java - 意图树懒加载（写时失效 + 读时重建）
private IntentTreeData loadIntentTreeData() {
    // 1. 尝试从 Redis 读取缓存（命中则直接返回，不查 MySQL）
    IntentTreeData cached = intentTreeCache.get();
    if (cached != null) {
        return cached;
    }

    // 2. 缓存 Miss，从数据库加载全量意图树
    List<IntentNode> allNodes = loadIntentTreeFromDB();

    // 3. 构建内存树形结构（id→节点映射 + 叶子节点列表）
    IntentTreeData data = buildTreeData(allNodes);

    // 4. 写入 Redis 缓存（TTL=30分钟，到期自动失效）
    intentTreeCache.set(data);
    return data;
}

// IntentTreeServiceImpl - 写时主动失效（增删改节点后调用）
private void evictCache() {
    redissonClient.getBucket("rag:intent:tree").delete();
    log.info("意图树缓存已失效，下次请求将从数据库重建");
}
```

> 完整意图分类实现参见：[RAG优化流程树与实现指南.md](./RAG优化流程树与实现指南.md) 第3节「意图识别」

---

## 五、文档定时刷新 (KnowledgeDocumentScheduleJob)

**核心类：**

| 类名 | 职责 |
|------|------|
| `KnowledgeDocumentScheduleJob` | 定时任务主体，扫描 + 调度 + 执行 |
| `RemoteFileFetcher` | 多源文件拉取（HTTP URL / 飞书 / S3），支持内容变化检测 |
| `KnowledgeDocumentScheduleDO` | 调度配置（t_knowledge_document_schedule）|
| `KnowledgeDocumentScheduleExecDO` | 每次执行的记录（t_knowledge_document_schedule_exec）|

**优化点：**

| 优化项 | 说明 | 核心代码位置 |
|--------|------|--------------|
| 数据库乐观锁防重 | 多实例部署时用 UPDATE CAS 抢占，只有一个实例执行 | `tryAcquireLock()` |
| 内容变化检测 | 对比 ETag / Last-Modified / ContentHash，未变直接 SKIP | `RemoteFileFetcher.fetchIfChanged()` |
| 锁续期 | 长耗时分块过程中定期续期分布式锁，防止锁超时被抢 | `renewLock()` |
| 崩溃恢复 | 每分钟扫描 RUNNING 超时的文档重置为 FAILED | `recoverStuckRunningDocuments()` |
| 文件原子替换 | 新文件分块成功后才替换旧文件 URL，失败则回滚 | `switchedToNewFile` 标志位 |
| 执行历史记录 | 每次调度写入执行记录，包含耗时、内容哈希、错误信息 | `execMapper.insert()` / `updateById()` |

**代码示例：**

> **业务例子（伪代码）**
>
> ```
> 场景：《员工手册》文档配置了定时刷新，Cron="0 2 * * *"（每天凌晨2点）
> 部署了2个服务实例：节点A（host-a），节点B（host-b）
>
> 凌晨2:00，两个实例同时触发 scan()：
>
>   节点A：
>   - 查询到 schedule 记录：id="sch_001"，nextRunTime=02:00，lockUntil=null
>   - 执行 tryAcquireLock("sch_001")
>   - UPDATE ... SET lockOwner="kb-schedule-host-a", lockUntil=02:05
>     WHERE (lockUntil IS NULL OR lockUntil < now)
>   - 受影响行数=1 → 抢锁成功
>   - 提交 executeSchedule("sch_001") 到线程池
>
>   节点B（几乎同时）：
>   - 查询到同一条 schedule 记录（lockUntil 还是 null，因为事务还未提交？不会）
>   - 执行 tryAcquireLock("sch_001")
>   - UPDATE 的 WHERE 条件：lockUntil IS NULL OR lockUntil < now
>   - 节点A已写入 lockUntil=02:05 > now=02:00 → 受影响行数=0 → 抢锁失败，跳过
>
>   节点A 执行 executeSchedule：
>   - renewLock()：续期到 02:10
>   - 拉取远程文件：GET https://corp.com/handbook.pdf
>   - 对比 ETag：响应 ETag="v20240315" vs 上次存储 ETag="v20240301"
>   - ETag 不同 → 内容变化，继续执行
>   - tryMarkDocumentRunning("doc_001") → 文档标记 RUNNING（CAS，防止并发分块）
>   - 上传新文件到 OSS
>   - renewLock()：续期到 02:15（分块可能耗时较长）
>   - 调用 chunkDocument() → 解析 + 分块 + 向量化 → 成功
>   - applyRefreshedFileMetadata() → 更新 fileUrl 到新文件（原子切换）
>   - switchedToNewFile = true
>   - markScheduleSuccess()：nextRunTime=明天凌晨2:00，lastStatus=SUCCESS
>   - releaseLock()：lockOwner=null
>   - finally: deleteOldFileQuietly(旧文件URL)（已成功才删旧文件）
>
> 输出：
>   t_knowledge_document_schedule：lastStatus=SUCCESS, nextRunTime=明天02:00
>   t_knowledge_document_schedule_exec：status=SUCCESS, durationMs=45000
> ```
>
> **这段代码做什么？**
>
> 这是"文档定时刷新"的完整逻辑，确保知识库内容自动保持最新：
> 1. **分布式锁抢占**：多实例环境通过数据库 CAS（Compare-And-Swap）操作确保只有一个实例执行，等同于数据库行级乐观锁
> 2. **内容变化检测**：通过 ETag、Last-Modified、ContentHash 三重校验，内容没变就 SKIP，避免无意义的重新入库
> 3. **锁续期机制**：分块过程可能很长（大文件数十秒），中途调用 `renewLock()` 延长锁有效期，防止被其他实例误抢
> 4. **文件原子替换**：只有在分块完全成功后才将文档的 `fileUrl` 切换到新文件，失败时回滚；`finally` 块只在成功时删旧文件，确保任何时刻知识库都有可用的向量数据

```java
// KnowledgeDocumentScheduleJob.java:98-133 - 扫描 + 分布式锁抢占
@Scheduled(fixedDelayString = "${rag.knowledge.schedule.scan-delay-ms:10000}")
public void scan() {
    Date now = new Date();
    // 查询：enabled=1 且 (nextRunTime 为空 或 nextRunTime <= now) 且 (lockUntil 为空 或 lockUntil < now)
    List<KnowledgeDocumentScheduleDO> schedules = scheduleMapper.selectList(
            new LambdaQueryWrapper<KnowledgeDocumentScheduleDO>()
                    .eq(KnowledgeDocumentScheduleDO::getEnabled, 1)
                    .and(w -> w.isNull(KnowledgeDocumentScheduleDO::getNextRunTime)
                            .or().le(KnowledgeDocumentScheduleDO::getNextRunTime, now))
                    .and(w -> w.isNull(KnowledgeDocumentScheduleDO::getLockUntil)
                            .or().lt(KnowledgeDocumentScheduleDO::getLockUntil, now))
    );

    Date lockUntil = new Date(System.currentTimeMillis() + scheduleProperties.getLockSeconds() * 1000L);
    for (KnowledgeDocumentScheduleDO schedule : schedules) {
        // CAS 抢占分布式锁（多实例环境只有一个成功）
        if (!tryAcquireLock(schedule.getId(), now, lockUntil)) {
            continue;
        }
        // 成功抢锁，提交异步执行
        knowledgeChunkExecutor.execute(() -> executeSchedule(schedule.getId()));
    }
}

// KnowledgeDocumentScheduleJob.java:414-424 - 数据库 CAS 分布式锁
private boolean tryAcquireLock(String scheduleId, Date now, Date lockUntil) {
    return scheduleMapper.update(
            Wrappers.lambdaUpdate(KnowledgeDocumentScheduleDO.class)
                    .set(KnowledgeDocumentScheduleDO::getLockOwner, instanceId)
                    .set(KnowledgeDocumentScheduleDO::getLockUntil, lockUntil)
                    .eq(KnowledgeDocumentScheduleDO::getId, scheduleId)
                    // 核心 CAS 条件：lockUntil 为空 或 已过期
                    .and(w -> w.isNull(KnowledgeDocumentScheduleDO::getLockUntil)
                            .or().lt(KnowledgeDocumentScheduleDO::getLockUntil, now))
    ) > 0;  // 受影响行数 > 0 才算抢锁成功
}

// KnowledgeDocumentScheduleJob.java:196-281 - 执行核心（含文件原子替换）
boolean switchedToNewFile = false;
try (RemoteFileFetcher.RemoteFetchResult fetchResult = remoteFileFetcher.fetchIfChanged(
        document.getSourceLocation(),
        schedule.getLastEtag(),         // 上次成功时存储的 ETag
        schedule.getLastModified(),     // 上次成功时的 Last-Modified
        schedule.getLastContentHash()   // 上次成功时的内容哈希
)) {
    if (!fetchResult.changed()) {
        markScheduleSkipped(...);  // 内容未变，记录 SKIPPED 状态
        return;
    }

    renewLock(scheduleId);  // 续期：后续操作耗时较长
    tryMarkDocumentRunning(document.getId());  // 文档 CAS 标记为 RUNNING

    stored = fileStorageService.upload(...);   // 上传新文件到 OSS

    renewLock(scheduleId);  // 再次续期（分块耗时）
    documentService.chunkDocument(runtimeDoc); // 重新分块入库

    // 分块成功后才原子切换文件 URL
    applyRefreshedFileMetadata(document.getId(), stored);
    switchedToNewFile = true;  // 标志位，finally 中根据此决策

    markScheduleSuccess(...);  // 记录成功，更新 nextRunTime
} finally {
    if (switchedToNewFile) {
        deleteOldFileQuietly(oldFileUrl, stored.getUrl());  // 成功才删旧文件
    } else if (stored != null) {
        deleteOldFileQuietly(stored.getUrl(), null);  // 失败则删新文件（回滚）
    }
    releaseLock(scheduleId);  // 释放锁
}
```

**使用场景：** 文档来源是外部 HTTP URL 或飞书文档时，配置定时刷新让知识库内容自动保持最新，无需人工重新上传。

**优化优点：**
- 数据库 CAS 替代 Redis 分布式锁，依赖组件少，不引入额外中间件
- 内容哈希三重校验（ETag + Last-Modified + ContentHash）杜绝无意义的重复入库
- 文件原子替换确保任何时刻知识库都有可用的向量数据，不出现"半换"状态

---

## 六、分块管理 (KnowledgeChunkService)

分块是 RAG 系统中最小的检索单元。每个分块对应 MySQL 中一条 `t_knowledge_chunk` 记录，同时在 Milvus 中有一条对应的向量数据。

**CRUD 接口：**

| 操作 | 方法 | 说明 |
|------|------|------|
| 查看分块 | `pageQuery(docId, request)` | 分页查看文档的所有分块内容 |
| 新增分块 | `create(docId, request)` | 手动添加分块（同步写入 Milvus 向量） |
| 编辑分块 | `update(docId, chunkId, request)` | 修改分块文本（同步更新 Milvus 向量） |
| 删除分块 | `delete(docId, chunkId)` | 删除分块（同步删除 Milvus 向量） |
| 批量启用 | `batchEnable(docId, request)` | 控制分块参与检索（不删向量） |
| 批量禁用 | `batchDisable(docId, request)` | 禁用分块（向量仍在，可重新启用） |
| 重建索引 | `rebuildByDocId(docId)` | 清空旧向量，对所有启用分块重新生成向量 |

**重建向量索引（关键操作）：**

> **业务例子（伪代码）**
>
> ```
> 场景：知识库更换了嵌入模型（1536维 → 3072维），旧向量维度不匹配，需要重建
>
> 调用 rebuildByDocId("doc_001")：
>
>   第1步：删除旧向量
>   - 调用 VectorStoreService.deleteDocumentVectors("kb_001_collection", "doc_001")
>   - Milvus 删除 doc_001 对应的所有 1536维 向量
>
>   第2步：查询所有启用的分块
>   - SELECT * FROM t_knowledge_chunk WHERE docId="doc_001" AND enabled=1
>   - 返回 25 条 KnowledgeChunkDO 记录
>
>   第3步：批量重新向量化并写入
>   - 25个分块文本 → 调用新嵌入模型 → 25个 3072维 向量
>   - 批量写入 Milvus：insertBatch()
>
> 输出：doc_001 的向量已用新模型重建，可正常参与检索
> ```
>
> **这段代码做什么？**
>
> `rebuildByDocId` 是"原地重建向量索引"的操作：
> 1. **删旧建新**：先清空 Milvus 中该文档的所有向量，再用当前启用分块重新生成
> 2. **不影响 MySQL**：只操作 Milvus 向量数据，MySQL 中的分块文本内容不变
> 3. **仅重建启用分块**：已禁用的分块（enabled=0）不参与重建，也不会出现在检索结果中

**数据模型：**

```java
// KnowledgeChunkDO - 分块数据表 (t_knowledge_chunk)
private String id;           // 分块ID（同时作为 Milvus 的 chunkId）
private String kbId;         // 所属知识库ID
private String docId;        // 所属文档ID
private Integer chunkIndex;  // 在文档中的序号（0开始）
private String content;      // 分块文本内容
private String contentHash;  // 内容哈希（去重用）
private Integer charCount;   // 字符数
private Integer tokenCount;  // Token 数（估算）
private Integer enabled;     // 是否启用：1=参与检索，0=禁用
```

**使用场景：**
- 更换嵌入模型后重建所有文档向量
- 手动编辑分块内容后刷新向量（如修正 OCR 错误）
- 批量禁用低质量分块（如纯页眉页脚）降低检索噪声

---

## 七、附录：关键数据库表

| 表名 | Entity 类 | 用途 |
|------|-----------|------|
| `t_knowledge_base` | `KnowledgeBaseDO` | 知识库主表 |
| `t_knowledge_document` | `KnowledgeDocumentDO` | 文档元数据（状态、文件URL、分块策略） |
| `t_knowledge_chunk` | `KnowledgeChunkDO` | 分块文本数据（对应 Milvus 向量） |
| `t_knowledge_document_chunk_log` | `KnowledgeDocumentChunkLogDO` | 分块处理日志 |
| `t_knowledge_document_schedule` | `KnowledgeDocumentScheduleDO` | 定时刷新配置 |
| `t_knowledge_document_schedule_exec` | `KnowledgeDocumentScheduleExecDO` | 定时执行历史记录 |
| `t_intent_node` | `IntentNodeDO` | 意图树节点数据 |

---

## 八、附录：核心文件索引

| 模块 | 类名 | 路径 |
|------|------|------|
| 文档服务 | `KnowledgeDocumentServiceImpl` | `bootstrap/.../knowledge/service/impl/KnowledgeDocumentServiceImpl.java` |
| 摄入引擎 | `IngestionEngine` | `bootstrap/.../ingestion/engine/IngestionEngine.java` |
| 解析节点 | `ParserNode` | `bootstrap/.../ingestion/node/ParserNode.java` |
| 分块节点 | `ChunkerNode` | `bootstrap/.../ingestion/node/ChunkerNode.java` |
| 向量节点 | `IndexerNode` | `bootstrap/.../ingestion/node/IndexerNode.java` |
| 知识库服务 | `KnowledgeBaseService` | `bootstrap/.../knowledge/service/KnowledgeBaseService.java` |
| 意图树服务 | `IntentTreeServiceImpl` | `bootstrap/.../ingestion/service/impl/IntentTreeServiceImpl.java` |
| 定时刷新 | `KnowledgeDocumentScheduleJob` | `bootstrap/.../knowledge/schedule/KnowledgeDocumentScheduleJob.java` |
| 分块服务 | `KnowledgeChunkService` | `bootstrap/.../knowledge/service/KnowledgeChunkService.java` |
| 向量存储 | `MilvusVectorStoreService` | `bootstrap/.../rag/core/vector/MilvusVectorStoreService.java` |
